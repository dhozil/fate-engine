export function Footer() {
  return (
    <footer className="border-t border-border/60 px-6 py-10">
      <div className="mx-auto flex max-w-[1500px] flex-col items-center justify-between gap-3 text-xs tracking-[0.14em] text-muted-foreground uppercase sm:flex-row">
        <span>Fate Engine — Built on GenLayer AI Consensus</span>
        <span>Probabilistic simulation. Not fortune telling.</span>
      </div>
    </footer>
  );
}
